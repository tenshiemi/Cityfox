%body
	.fullscreen.background.hero
		= render "layouts/header"
		.container
			.content--hero
				%h2 Unsure about a new client?
				%h2 We've got your back.
				.search
					= form_tag search_path, :method => :post do
						= search_field_tag :search_term, nil, :placeholder => 'Find a company...'
				%h3 Reviews for freelancers, design and development shops, and anyone who does client work.
