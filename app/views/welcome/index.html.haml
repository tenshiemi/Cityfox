%body
	.hero
		= render "layouts/header"
		.container
			%p#notice
				= flash[:notice]
			%p#alert
				= flash[:alert]
			= form_tag search_path, :method => :post do
				= search_field_tag :search_term
